import type { Product } from "@/types/inventory"

type pageProps = {
    product: Product
}

export default function EditProductDetails({product}:pageProps) {
    return (
        <div></div>
    )
}
